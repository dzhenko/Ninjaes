var expect = require('chai').expect,
    battleHandler = require('../handlers/battleHandler');

describe('battleHandler', function () {
   it('just for the test', function () {
       console.log(battleHandler);
       expect(true).to.be.true;
   })
});